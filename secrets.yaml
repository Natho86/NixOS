# Example secrets file for sops-nix
# After setting up age keys, encrypt this file with:
# sops secrets.yaml

# Example secrets (these will be encrypted)
example:
  api_key: "your-api-key-here"
  password: "your-password-here"

# WiFi passwords
wifi:
  home: "your-wifi-password"
  work: "your-work-wifi-password"

# SSH keys or other sensitive data can be stored here
